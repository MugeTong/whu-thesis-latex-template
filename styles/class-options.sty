% 本文件定义了模板的选项, 用于控制模板的一些行为.
\ProvidesPackage{styles/class-options}[
    2025/03/12
    v0.1.0
    Options for the thesis template.
]

% 选项名称       类型           默认值      说明
% forprint      Store True     -          交付打印时添加, 避免彩色链接字迹打印偏淡
% anonymous     Store True     -          盲审时添加, 隐藏作者信息
% addnocite     Store True     -          显示所有参考文献, 默认仅显示引用的文献
%
%
%



% 定义打印选项, 隐藏超链接颜色
\newif\ifprint\printfalse
\DeclareOption{forprint}{\printtrue}
\ProcessOptions\relax
\ifprint
    % 启用打印时, 隐藏超链接颜色
    \hypersetup{hidelinks}
\else
    % 未启用时, 引用颜色为洋红色, 链接颜色为蓝色
    \hypersetup{colorlinks,citecolor=magenta,linkcolor=blue}
\fi

% 定义匿名选项, 隐藏作者信息
\newif\ifanonymous\anonymousfalse
\DeclareOption{anonymous}{\anonymoustrue}
\ProcessOptions\relax
% 记录是否需要清空
\newcommand{\isanonymous}[2]{%
    \ifanonymous
        #1
    \else
        #2
    \fi
}

% 定义引用选项, 显示所有bib文件中的文献
\newif\ifaddnocite\addnocitefalse
\DeclareOption{addnocite}{\addnocitetrue}
\ProcessOptions\relax
